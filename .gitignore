# Build artifacts
build/
dist/
.build/
*.dmg

# Signing material: never commit private keys or notary credentials
*.p12
*.pem
*.cer
*.p8
*.key
*.mobileprovision
*.keychain-db

# Local-only working notes
AGENTS.md
CLAUDE.md
E2E/

# Agent working documents (audit ledgers, handoff briefs, research notes)
AUDIT_ARCHIVE.md
HANDOFF_*.md
KEXT_FINDINGS.md
KEXT_WAVE_PLAN.md
ZEN4_CURVE_OPTIMIZER_RESEARCH.md
CROSS_DRIVER_IDEA_MINE.md

# Freebuff desktop cache (tool internal state, never committed)
.freebuff/

# Audit prompt for Claude — internal only, never push to GitHub
.claude-audit-prompt.md

# macOS noise
.DS_Store

# Xcode / SwiftPM user state & DerivedData
*.xcodeproj
xcuserdata/
.swiftpm/
DerivedData/
build_output.log
*.log

# Internal agent plans & artifacts
task_plan.md
implementation_plan.md
walkthrough.md
task.md
docs/superpowers/
.gemini/
.agents/
.Jules/
.jules/
.user_uploaded/
.system_generated/
.research/

# Upstream sync tooling (local-only: holds the upstream repo URL; never pushed)
.upstream-sync/


# Merge/diff backup artifacts (never commit)
*.orig
