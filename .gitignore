# Build artifacts
build/
dist/
.build/
*.dmg

# Signing material: never commit private keys or notary credentials
*.p12
*.pem
*.cer
*.p8
*.key
*.mobileprovision
*.keychain-db

# Local-only working notes
AGENTS.md
CLAUDE.md
E2E/

# Freebuff desktop cache (tool internal state, never committed)
.freebuff/

# Audit prompt for Claude — internal only, never push to GitHub
.claude-audit-prompt.md

# macOS noise
.DS_Store

# Xcode / SwiftPM user state
*.xcodeproj
xcuserdata/
.swiftpm/
build_output.log
